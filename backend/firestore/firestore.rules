rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    match /users/{userDoc}/dropInReservations/{reservationDoc} {
      allow read: if userDoc == request.auth.uid;
    }
    match /users/{userDoc}/bookingReservations/{reservationDoc} {
      allow read: if userDoc == request.auth.uid;
    }
    match /locations/{locationDoc}/dropInEvents/{eventDoc} {
      allow read: if true;
    }
    match /locations/{locationDoc}/bookingEvents/{eventDoc} {
      allow read: if true;
    }
  }
}