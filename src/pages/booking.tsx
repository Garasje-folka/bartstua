import { CardContainer, CardHeader } from "../components/card";

const Booking = () => {
  return (
    <CardContainer>
      <CardHeader title="Booking" />
    </CardContainer>
  );
};

export default Booking;
